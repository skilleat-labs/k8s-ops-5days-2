apiVersion: v1
kind: Pod
metadata:
  name: crashloop
  namespace: pod-deepdive
spec:
  containers:
  - name: worker
    image: busybox:1.36
    command: ['sh','-c','echo "starting..."; sleep 1; exit 1']
    # 의도적으로 짧게 실패 → kubelet이 재시작, CrashLoopBackOff 진입

